"""{{title}} — concept {{NN}}-{{slug}} of the {{graph}} learning map.

{{1-2 sentence plain-English summary of what this script demonstrates.}}

This script is the runnable code analog of `concepts/{{NN}}-{{slug}}.md`.
CPU-runnable, <30 s, ≤80 lines. Stdlib + numpy/torch only; no project imports.

For abstract concepts (sigma-algebras, continuity at a point, etc.) implement
a finite or discrete witness that captures the concept's essence.
"""


def main():
    print("{{title}} (concept {{NN}}-{{slug}})")
    print("=" * 60)
    # TODO: demonstrate the concept on a concrete example.
    # Print explanatory messages so a reader of the .md can interpret stdout.
    raise NotImplementedError(
        "Replace this with a runnable demonstration of the concept."
    )


if __name__ == "__main__":
    main()
